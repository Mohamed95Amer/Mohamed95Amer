# Majal API

Integration documentation for **Majal**, a Construction & Facilities Management ERP
built on **Odoo 18 Community**.

Majal exposes two distinct surfaces:

1. **Odoo's standard external API** — XML-RPC and JSON-RPC. This is the whole ORM:
   every Majal model, field and public method. It is what you will use for 95% of
   integrations.
2. **Majal's own HTTP controllers** — a small, deliberate set of routes for asset
   tag scanning, the offline field app, the customer/subcontractor portal, CSV
   export and the WhatsApp webhook.

There is no separate REST API, no GraphQL endpoint, and no `/api/v1/...` namespace.
If a route is not listed in [Custom endpoints](endpoints/index.md), it does not exist.

## Start here

| Page | What it covers |
| --- | --- |
| [Getting started](getting-started.md) | Base URLs, obtaining an API key, your first authenticated call |
| [Conventions](conventions.md) | IDs, domains, pagination, dates and time zones, multi-company, `Command` tuples |
| [XML-RPC](xmlrpc.md) | `common`/`object` services, `authenticate`, `execute_kw` |
| [JSON-RPC](jsonrpc.md) | `/jsonrpc`, session auth, `/web/dataset/call_kw` |
| [Errors](errors.md) | Fault codes, exception classes, and what each one means |

## Domain reference

| Page | Models |
| --- | --- |
| [Projects](models/projects.md) | `project.project` construction extensions, the document mixin |
| [Commercial](models/commercial.md) | BOQ, progress claims, change orders, tenders, subcontracts |
| [Site](models/site.md) | RFIs, submittals, drawings, defects, daily logs, inspections, pins, materials |
| [HSE](models/hse.md) | Permits to work, incidents, toolbox talks, project safety statistics |
| [Facilities](models/facilities.md) | Locations, assets, work orders, SLAs, maintenance contracts, spare parts |
| [Documents](models/documents.md) | Controlled documents, versions, templates, structured sheets |
| [BIM](models/bim.md) | Models, elements, properties, pins, clashes, comparisons |
| [Messaging](models/messaging.md) | WhatsApp accounts, templates, message log |

## Behaviour you must read before writing code

| Page | Why |
| --- | --- |
| [Approvals](approvals.md) | Approvals are enforced server-side. Direct writes to `construction.approval.step` raise `AccessError`. This is the single most common integration mistake. |
| [Security model](security.md) | Groups, record rules, what a portal user can and cannot reach, and which tokens are secrets. |
| [Custom endpoints](endpoints/index.md) | Every `@http.route` in the product, with auth level, methods and CSRF posture. |
| [Webhooks](webhooks.md) | The WhatsApp inbound callback — the only inbound webhook in the product. |

## Version

- Odoo **18.0** Community (vendored; see `ODOO_PINNED_SHA` in the repository root).
- Majal addon manifests are versioned `18.0.1.0.0`.
- See the [changelog](changelog.md).

## Conventions used in these docs

Placeholders are always written in this form and must be replaced:

- `https://your-host` — the Majal base URL
- `YOUR_DATABASE` — the database name (Majal ships single-database; commonly `erp`)
- `YOUR_LOGIN` — the user login (an e-mail address)
- `YOUR_API_KEY` — a developer API key generated by that user

No sample in these docs contains a real credential, hostname or token.

