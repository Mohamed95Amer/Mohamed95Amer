<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_majal_offline_operation_owner" model="ir.rule">
        <field name="name">Offline operations: own records</field>
        <field name="model_id" ref="model_majal_offline_operation"/>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="domain_force">[('user_id', '=', user.id), ('company_id', 'in', company_ids)]</field>
    </record>
</odoo>
