<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- The action lives with the component. Each suite hangs its own menu
         item off its own root, since the shell does not know what roots
         exist. -->
    <record id="action_my_day" model="ir.actions.client">
        <field name="name">My Day</field>
        <field name="tag">majal_suite_ui.my_day</field>
    </record>
</odoo>
