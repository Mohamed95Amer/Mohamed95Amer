<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sla_policy_form" model="ir.ui.view">
        <field name="name">facility.sla.policy.form</field>
        <field name="model">facility.sla.policy</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_requests" type="object"
                                class="oe_stat_button" icon="fa-wrench">
                            <field name="request_count" widget="statinfo"
                                   string="Work Orders"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1><field name="name"
                                   placeholder="e.g. Critical assets — 1h response"/></h1>
                    </div>
                    <group>
                        <group string="Applies to (empty = any)">
                            <field name="priority"/>
                            <field name="maintenance_type"/>
                            <field name="category_id"/>
                            <field name="criticality"/>
                        </group>
                        <group string="Commitment">
                            <field name="response_hours"/>
                            <field name="resolution_hours"/>
                            <field name="calendar_id"/>
                            <field name="at_risk_ratio"/>
                        </group>
                    </group>
                    <group>
                        <field name="sequence"/>
                        <field name="company_id" groups="base.group_multi_company"/>
                        <field name="active" invisible="1"/>
                    </group>
                    <div class="text-muted">
                        Policies are evaluated in sequence order and the first
                        match wins, so keep the catch-all last.
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_sla_policy_list" model="ir.ui.view">
        <field name="name">facility.sla.policy.list</field>
        <field name="model">facility.sla.policy</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="priority"/>
                <field name="maintenance_type"/>
                <field name="category_id"/>
                <field name="criticality"/>
                <field name="response_hours"/>
                <field name="resolution_hours"/>
                <field name="calendar_id"/>
                <field name="request_count" string="Work Orders"/>
            </list>
        </field>
    </record>

    <record id="action_sla_policy" model="ir.actions.act_window">
        <field name="name">SLA Policies</field>
        <field name="res_model">facility.sla.policy</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Define your first SLA</p>
            <p>An SLA policy sets the response and resolution allowance for a
               class of work orders, measured on a business calendar. The first
               policy whose criteria match a work order is the one applied.</p>
        </field>
    </record>
</odoo>
